<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Language</title>
  </head>
  <body>
    <form action="/language" method="post">
      Languages: <br />

      <input type="checkbox" name="language" value="TEL"
      <%=helper.isChecked('TEL') %>/> Telegu<br />
      <input type="checkbox" name="language" value="ENG"
      <%=helper.isChecked('ENG') %>/> English <br />
      <input type="checkbox" name="language" value="HIN"
      <%=helper.isChecked('HIN') %>/>Hindi <br />
      <input type="checkbox" name="language" value="KAN"
      <%=helper.isChecked('KAN') %>/> Kannada <br />

      <input type="submit" value="Submit" />
    </form>
    <br />
    <% if(languages.length > 0) { %> <%= languages %> Selected <% } %>
  </body>
</html>
